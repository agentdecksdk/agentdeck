{/* docs_sources:
  - "agentdeck/authoring/interrupts.py"
  - "agentdeck/authoring/nodes.py"
  - "agentdeck/core/control.py"
  - "agentdeck/adapters/engines/langgraph/**"
*/}

# Human Input

Suspend execution for human approval or additional inputs.

## Answering Prompts

When a run enters `WAITING`, supply input with `run.answer()`:

```python
await run.answer({"approved": True})
```
